# Core Django
Django==4.2.7
python-dotenv==1.0.0

# Database
# For production MySQL support
# Option 1: mysqlclient (requires compilation - may fail on shared hosting)
# mysqlclient==2.2.0
# Option 2: PyMySQL (pure Python, works on shared hosting)
PyMySQL==1.1.0
cryptography==43.0.0  # Required for PyMySQL with MySQL 8.0+
# SQLite is built-in, add PostgreSQL if needed:
# psycopg2-binary==2.9.9

# Security
django-cors-headers==4.3.1

# Celery for async tasks (optional - NOT USED on shared hosting)
# celery==5.3.4
# redis==5.0.1

# Django Channels (WebSocket - using in-memory on shared hosting)
channels==4.0.0
daphne==4.0.0

# Data processing
pandas==2.1.3
numpy==1.26.2

# API
requests==2.31.0

# Testing
pytest==7.4.3
pytest-django==4.7.0

# Code quality
flake8==6.1.0
black==23.12.0

# Production server
gunicorn==21.2.0
whitenoise==6.6.0

# ZenNews dependencies - Financial news integration
feedparser==6.0.10
spacy==3.7.2
textblob==0.17.1
vaderSentiment==3.3.2
scikit-learn==1.3.2
python-dateutil==2.8.2
nltk==3.8.1
